#def (identifica una función nueva), siempre termina con dos puntos 
#return identifica que hará la función con las variables que le otrogues
#declarar las variables dentro del def 
#establecer si quiero que me devuelva algo o no mi funcion 
#los parametros dentro de mi def deben estar en el orden adecuado
#labeled values son valores que ya estan definidos dentro de mi funcion
#puedo alterar mis labeled values pero dentro de mi print y dentro del orden que ya le habia asignado 
# en el caso que al declarar mi funcion sean todos labeled values da igual el orden dentro de mi print 
#si quiero imprimirlos tal cual dejo el parentesis en vacio
#si no le asignas return a tu funcion te dara none'
#args(*) mete a los valores en una tupla
#kwargs(**)